import { Component} from "@angular/core";

@Component({
    selector: "foreach-components-component",
    templateUrl: "foreach-components.component.html"
})

export class ForeachComponentsComponent {
}
